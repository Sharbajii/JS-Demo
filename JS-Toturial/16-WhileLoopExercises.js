        //    var school = ["Anas", "Anna", "Bara", "Attila", "Khaled", "Wahed", "Waled", "Mohammad"]
    
        //    for (var memberIndex = 0; memberIndex < school.length; memberIndex++){
        //        document.getElementById("school").innerHTML += school[memberIndex] + " -- "
        //    }
        
        //     document.getElementById("search").addEventListener("click", function(){
        //         var userInput = document.getElementById("student-Input").value.toLowerCase();
        //         var studentName = 0
                
                /*while with break

                while(studentName < school.length){
                        var student = school[studentName]
                        var loweredStudent = student.toLowerCase()
                     if(userInput == loweredStudent){
                          document.getElementById("result").innerHTML = `${userInput} is in the school`
                        break
                     }
                     studentName++
                     document.getElementById("result").innerHTML = `${userInput} is NOT in the school`
                    }*/
    
                    /*while with  continue & break
                    
                while(studentName < school.length){
                    var student = school[studentName]
                    var loweredStudent = student.toLowerCase()
                    document.getElementById("result").innerHTML = `${userInput} is NOT in the school`
    
                    studentName++
                    if(userInput != loweredStudent){
                        continue
                    }
                    document.getElementById("result").innerHTML = `${userInput} is in the school`
                    break
                }*/
                //})
      